# Sample Markdown File

This is a **sample markdown** file to test the Glance preview.

## Features

- [x] Markdown rendering
- [x] Syntax highlighting
- [ ] More features coming

## Code Example

```typescript
interface User {
  name: string;
  email: string;
}

function greet(user: User): string {
  return `Hello, ${user.name}!`;
}
```

## Table

| Feature | Status |
|---------|--------|
| Markdown | ✅ |
| TSX/JSX | ✅ |
| HTML | ✅ |
| SVG | ✅ |

> This is a blockquote to test styling.

---

That's all for now!
